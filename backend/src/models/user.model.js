// User model — per NOSTRUM-DESIGN.md §15 data model:
// User (id, email, name, role: consumer|admin, locale, created_at)
// TODO: define with the chosen ORM (Prisma/Drizzle) or raw queries.

module.exports = {};
